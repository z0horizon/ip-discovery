[package]
name = "ip-discovery-node"
version = "0.5.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3.4", features = ["async", "tokio_rt"] }
napi-derive = "3.3"
ip-discovery = { path = "..", features = ["all"] }
